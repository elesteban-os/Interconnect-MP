#include "cache_class.cpp"

// Clase PE (Processing Element)
class PE {
public:
    /* atributos de la clase */
    int id;
    Cache cache;

    PE(int id) : id(id) {} // constructor

    /* métodos de la clase */
};